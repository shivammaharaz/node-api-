{
  "name": "api-deploy",
  "version": "1.0.0",
  "description": "first api deploy on heroku",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "shivam",
  "license": "ISC",
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.18.1",
    "gh-pages": "^4.0.0"
  }
}
